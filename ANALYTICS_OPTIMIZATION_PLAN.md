# 後台統計系統 - 優化計劃

**計劃日期**: 2025-11-14
**優化目標**: 根據現狀驗證結果，制定優化策略
**預計工時**: 1-2 週（取決於決策）

---

## 📋 第 1 部分：優化前提條件

### 前置檢查清單

在開始優化前，必須完成以下驗證：

| 項目 | 優先級 | 預計時間 | 執行者 |
|------|--------|---------|--------|
| 確認追蹤數據為何不完整 (Network 測試) | 🔴 高 | 1 小時 | 開發人員 |
| 創建 feedback 表 | 🔴 高 | 0.5 小時 | 開發人員 |
| 澄清 Section 追蹤是否重複 | 🟡 中 | 0.5 小時 | 開發人員 |
| **詢問利益相關者是否需要統計** | 🔴 高 | 1 小時 | 產品經理 |

### 前置檢查完成後的依賴決策

```
根據利益相關者反饋:
├─ 如果說「不需要統計功能」
│  └─ 執行【方案 A: 刪除】
├─ 如果說「可能以後需要，但優先級低」
│  └─ 執行【方案 B: 保留並標記】
└─ 如果說「確實需要這些報告」
   └─ 執行【方案 C: 實現儀表板】
```

---

## 📊 第 2 部分：三個優化方案詳細規劃

### 方案 A: 刪除未使用的統計端點（激進方案）

**適用場景**: 確信 20+ 統計端點在未來 6 個月都不會被使用

**優化內容**:

```
刪除項目清單:
├─ LLM 相關端點 (27 行)
│  └─ GET /llm-stats
├─ 供應商相關端點 (16 行)
│  └─ GET /provider-comparison
├─ FAQ 見解端點 (62 行)
│  └─ GET /faq-insights
├─ 熱門查詢端點 (18 行)
│  └─ GET /popular-queries
├─ 成本報告端點 (49 行)
│  └─ GET /cost-report
├─ 數據導出端點 (42 行)
│  └─ GET /export
├─ 警報系統 (109 行)
│  ├─ GET /alerts/check
│  ├─ GET /alerts/status
│  ├─ PUT /alerts/config
│  └─ AlertService 類及初始化 (75 行)
├─ 標籤統計端點 (114 行)
│  └─ GET /tag-stats
├─ 其他未使用的統計端點 (~800 行)
│  └─ /faq-stats, /section-stats, /resort-stats, /feedback-stats 等
└─ Section 追蹤重複 (32 行)
   └─ 刪除 track-section-view，保留 track-section-click
```

**詳細任務**:

#### A1: 列出完整刪除清單 (0.5 小時)
```bash
# 1. 在 analytics.js 中標記出所有要刪除的端點
# 2. 統計每個端點的行數
# 3. 生成刪除清單文檔
```

**待辦**:
- [ ] 掃描 analytics.js，列出所有要刪除的端點位置
- [ ] 計算總行數
- [ ] 生成刪除前清單

#### A2: 刪除統計端點 (2 小時)
```bash
# 1. 備份原始文件
# 2. 刪除以下端點:
#    - /llm-stats
#    - /provider-comparison
#    - /faq-insights
#    - /popular-queries
#    - /cost-report
#    - /export
#    - 所有 /alerts/* 端點
#    - /tag-stats
#    - /faq-stats (如果確認未使用)
#    - /section-stats (如果確認未使用)
#    - 等等其他未使用的端點
# 3. 刪除對應的 Service 類
```

**待辦**:
- [ ] 備份 `zeabur_backend/backend/src/routes/analytics.js`
- [ ] 逐個刪除未使用的端點
- [ ] 刪除相應的邏輯函數
- [ ] 刪除 AlertService 等相關類

#### A3: 刪除重複的追蹤端點 (1 小時)
```bash
# 刪除 track-section-view，保留 track-section-click
# 理由: 功能相同，保留更具描述性的名稱
```

**待辦**:
- [ ] 確認 `track-section-view` 和 `track-section-click` 的確完全相同
- [ ] 刪除 `track-section-view` 後端實現
- [ ] 刪除前端中 `track-section-view` 的調用（如有）

#### A4: 清理數據庫表 (0.5 小時)
```bash
# 刪除 SQLite 中對應的表
sqlite3 data/analytics.db "DROP TABLE IF EXISTS tag_clicks;"
sqlite3 data/analytics.db "DROP TABLE IF EXISTS provider_stats;"
sqlite3 data/analytics.db "DROP TABLE IF EXISTS llm_usage;"
```

**待辦**:
- [ ] 備份 analytics.db
- [ ] 識別哪些表可以安全刪除
- [ ] 執行刪除
- [ ] 驗證應用仍然運作

#### A5: 測試和驗證 (2 小時)
```bash
# 1. 啟動後端伺服器
# 2. 測試保留的端點:
#    - GET /api/v1/analytics/hot-faqs
#    - POST /api/v1/analytics/track-faq-view
#    - POST /api/v1/analytics/track-section-click
#    - POST /api/v1/analytics/track-tag-click
#    - POST /api/v1/analytics/track-resort-click
#    - POST /api/v1/analytics/track-resort-engagement
#    - POST /api/v1/analytics/feedback
# 3. 驗證前端功能正常
```

**待辦**:
- [ ] 啟動本機後端伺服器
- [ ] 測試所有 7 個保留的追蹤端點
- [ ] 測試 /hot-faqs 統計端點
- [ ] 驗證前端側邊欄「熱門問題」正常顯示
- [ ] 檢查瀏覽器 Console 無錯誤

#### A6: 文檔更新 (0.5 小時)
```bash
# 1. 更新 README.md 的 API 文檔
# 2. 添加遷移說明 (v1 → v2)
# 3. 列出已刪除的功能
```

**待辦**:
- [ ] 更新 API 文檔
- [ ] 添加「API v2.0 遷移指南」
- [ ] 列出已棄用的端點和替代方案

**方案 A 統計**:
- 刪除代碼: ~1,300 行 (68%)
- 保留代碼: ~600 行 (32%)
- 預計時間: **5.5 小時**
- 風險等級: 🟢 低 (刪除的都是未使用的)
- 優勢: 代碼簡潔，維護負擔大幅降低

---

### 方案 B: 保留並標記為未使用（保守方案）

**適用場景**: 不確定將來是否需要，想保留選項

**優化內容**:

#### B1: 代碼標記 (2 小時)
```javascript
/**
 * DEPRECATED: This endpoint is not currently used.
 * 已棄用：此端點目前未使用
 *
 * 背景: 根據 2025-11-14 分析，以下統計端點已實現但未被調用:
 * - /faq-stats: FAQ 點擊統計
 * - /section-stats: Section 點擊統計
 * - /tag-stats: 標籤點擊統計
 * - /resort-stats: Resort 點擊統計
 * - /feedback-stats: 反饋統計
 * 等 20+ 個端點
 *
 * 為什麼保留:
 * - 代碼已完全實現，邏輯正確
 * - 可能在未來需要（如實現儀表板）
 * - 直接刪除時如需恢復要重新編寫
 *
 * 使用方式:
 * 如果需要這些端點，取消此標記並進行測試
 */
router.get('/faq-stats', async (req, res) => {
  // ... 實現保持不變
});
```

**待辦**:
- [ ] 掃描 analytics.js 中所有未使用的端點
- [ ] 為每個端點添加 DEPRECATED 註釋
- [ ] 詳細說明為什麼保留
- [ ] 說明如何重新啟用

#### B2: 文檔化 (1.5 小時)
```markdown
# 備用統計端點清單

## 已實現但未使用的端點

### 1. FAQ 統計
- 端點: GET /api/v1/analytics/faq-stats
- 參數: faq_id, days, limit, language
- 返回: FAQ 的點擊統計和排行
- 狀態: 已實現, 未使用
- 啟用方式: 如需儀表板, 可直接調用

### 2. Section 統計
- 端點: GET /api/v1/analytics/section-stats
...
```

**待辦**:
- [ ] 創建「DEPRECATED_ENDPOINTS.md」文檔
- [ ] 詳細列出所有 20+ 個未使用的端點
- [ ] 說明各端點的功能和參數
- [ ] 提供啟用指南

#### B3: 代碼組織 (1 小時)
```javascript
// analytics.js 結構:

// 第 1 部分: 活躍端點 (使用中)
// └─ /hot-faqs
// └─ /track-faq-view
// └─ /track-section-click
// ...

// 第 2 部分: 備用端點 (未使用，已棄用)
// 標記為: // ========== DEPRECATED ENDPOINTS ==========
// └─ /faq-stats
// └─ /section-stats
// ...
```

**待辦**:
- [ ] 重新組織 analytics.js 的代碼結構
- [ ] 將活躍端點和備用端點分開
- [ ] 添加清晰的分隔註釋

#### B4: 測試 (1 小時)
```bash
# 驗證:
# 1. 活躍端點仍然工作
# 2. 備用端點仍然可調用 (如需)
```

**待辦**:
- [ ] 測試活躍的 7 個追蹤端點
- [ ] 測試 /hot-faqs
- [ ] 選擇 1-2 個備用端點測試其是否仍可調用

**方案 B 統計**:
- 代碼改動: ~100 行 (註釋和重組)
- 刪除代碼: 0 行
- 保留代碼: ~1,900 行
- 預計時間: **5.5 小時**
- 風險等級: 🟢 低 (只是註釋和重組)
- 優勢: 保留所有功能，以後無需重新實現

---

### 方案 C: 實現儀表板（完整方案）

**適用場景**: 確認利益相關者需要統計報告和決策支持

**優化內容**:

這是最複雜的方案，需要創建展示層和行動層。

#### C1: 需求定義 (4 小時)
```
詢問的關鍵問題:
1. 誰應該使用儀表板? (CRM 經理、營銷團隊、FAQ 編寫者)
2. 需要看什麼指標?
   - 最熱門的 FAQ?
   - 最常見的搜尋詞?
   - 反饋情緒分析?
   - Resort 興趣排行?
3. 多久更新一次? (實時、每小時、每天)
4. 儀表板在哪裡? (admin 頁面、CRM 整合、獨立應用)
5. 他們會用來做什麼決策?
```

**待辦**:
- [ ] 與 CRM 經理進行訪談 (1.5 小時)
- [ ] 與營銷團隊進行訪談 (1.5 小時)
- [ ] 記錄需求清單
- [ ] 設計儀表板的 UI/UX 草圖

#### C2: 設計儀表板 (3 小時)
```
儀表板結構 (示例):

┌─────────────────────────────────────┐
│  FAQ 系統分析儀表板                   │
├─────────────────────────────────────┤
│ 日期篩選: [_____] 至 [_____]          │
├─────────────────────────────────────┤
│ 【熱門 FAQ 排行】(過去 30 天)        │
│ 1. FAQ 標題 - 250 次點擊             │
│ 2. FAQ 標題 - 180 次點擊             │
│ ...                                  │
├─────────────────────────────────────┤
│ 【最常見搜尋詞】                     │
│ • 教練 (45 次)                       │
│ • 價格 (38 次)                       │
│ ...                                  │
├─────────────────────────────────────┤
│ 【Resort 興趣排行】                  │
│ Resort A ████████ 120 次              │
│ Resort B ██████ 85 次                │
│ ...                                  │
├─────────────────────────────────────┤
│ 【用戶反饋統計】                     │
│ ✅ 正面 (60%)  😊 中性 (25%)  ❌ 負面 (15%) │
└─────────────────────────────────────┘
```

**待辦**:
- [ ] 繪製儀表板草圖
- [ ] 決定顯示的指標
- [ ] 決定更新頻率
- [ ] 確認顏色和設計主題

#### C3: 後端實現 (6 小時)
```javascript
// 確保以下端點運作良好:

// 1. 熱門 FAQ (已有)
GET /api/v1/analytics/hot-faqs

// 2. 熱門搜尋詞 (需實現或重啟用)
GET /api/v1/analytics/popular-queries

// 3. Resort 興趣排行 (需實現或重啟用)
GET /api/v1/analytics/resort-stats

// 4. 反饋情緒分析 (需實現或重啟用)
GET /api/v1/analytics/feedback-stats

// 5. 時間範圍篩選 (優化現有端點以支持)
GET /api/v1/analytics/hot-faqs?start_date=2025-01-01&end_date=2025-11-14
```

**待辦**:
- [ ] 檢查現有端點是否支持時間範圍篩選
- [ ] 如果 /popular-queries 已刪除，重新實現
- [ ] 優化查詢效能 (緩存、索引)
- [ ] 添加速率限制以防止過度查詢

#### C4: 前端實現 (8 小時)
```html
<!-- 創建 admin/analytics.html -->
<!-- 或整合到 admin.html 中 -->

<div id="analytics-dashboard">
  <div class="date-filter">
    <input type="date" id="start-date">
    <input type="date" id="end-date">
    <button onclick="loadAnalytics()">載入</button>
  </div>

  <div class="metrics">
    <section id="hot-faqs-chart"></section>
    <section id="popular-queries-chart"></section>
    <section id="resort-interest-chart"></section>
    <section id="feedback-sentiment-chart"></section>
  </div>
</div>

<script>
  // 1. 載入數據
  async function loadAnalytics() {
    const startDate = document.getElementById('start-date').value;
    const endDate = document.getElementById('end-date').value;

    const [hotFaqs, queries, resorts, feedback] = await Promise.all([
      fetch(`/api/v1/analytics/hot-faqs?start_date=${startDate}&end_date=${endDate}`),
      fetch(`/api/v1/analytics/popular-queries?start_date=${startDate}&end_date=${endDate}`),
      fetch(`/api/v1/analytics/resort-stats?start_date=${startDate}&end_date=${endDate}`),
      fetch(`/api/v1/analytics/feedback-stats?start_date=${startDate}&end_date=${endDate}`)
    ]);

    // 2. 渲染圖表 (使用 Chart.js 或 D3.js)
    renderCharts(hotFaqs, queries, resorts, feedback);
  }

  // 3. 定時更新 (可選)
  setInterval(loadAnalytics, 3600000); // 每小時更新
</script>
```

**待辦**:
- [ ] 創建 admin/analytics.html 或在 admin.html 中添加分析標籤頁
- [ ] 實現日期篩選功能
- [ ] 使用圖表庫 (Chart.js 或 D3.js) 渲染數據
- [ ] 添加導出功能 (CSV/PDF)
- [ ] 優化移動端設計

#### C5: CRM 整合 (4 小時)
```javascript
// 在 CRM 系統 (tagger.html) 中集成統計:

// 1. 在 FAQ 建議中顯示「熱門度」
async function suggestFAQs(intent, query) {
  // 獲取最相關的 FAQ
  const results = faqEngine.search(query, intent);

  // 獲取這些 FAQ 的點擊統計
  const hotFaqs = await fetch('/api/v1/analytics/hot-faqs?days=30');

  // 在建議中加入「這個 FAQ 很熱門」的標籤
  results.forEach(faq => {
    if (hotFaqs.includes(faq.id)) {
      faq.tag_hot = true; // 標記為熱門
      faq.view_count = ...;
    }
  });

  return results;
}

// 2. 根據反饋統計改進標籤規則
async function suggestTagRuleImprovements() {
  // 獲取最常見的反饋
  const feedback = await fetch('/api/v1/analytics/feedback-stats?days=30');

  // 分析反饋內容，建議改進的標籤規則
  const suggestions = analyzeCommonFeedback(feedback);

  return suggestions;
}
```

**待辦**:
- [ ] 在 CRM 的 FAQ 建議中顯示「熱門度」指標
- [ ] 實現「根據反饋改進標籤」的建議功能
- [ ] 添加「最常見問題」的快速提醒
- [ ] 在 CRM 儀表板中添加「本週熱點」小部件

#### C6: 測試和優化 (4 小時)
```bash
# 1. 功能測試
#    - 儀表板加載和渲染
#    - 日期篩選功能
#    - 圖表交互性
#    - 導出功能

# 2. 性能測試
#    - 頁面加載時間 < 2 秒
#    - 圖表渲染 < 1 秒
#    - API 響應 < 200 ms

# 3. 用戶測試
#    - 邀請 CRM 經理試用
#    - 收集反饋
#    - 迭代改進
```

**待辦**:
- [ ] 測試儀表板的所有功能
- [ ] 測試不同屏幕尺寸 (桌面、平板、手機)
- [ ] 性能優化 (快取、懶加載、虛擬滾動)
- [ ] 邀請實際用戶進行 UAT

**方案 C 統計**:
- 新增代碼: ~500 行 (前端) + ~200 行 (後端優化)
- 預計時間: **25-30 小時** (跨越 1 週)
- 風險等級: 🟡 中 (新功能需要充分測試)
- 優勢: 統計數據真正被利用，推動決策
- 後續成本: 定期維護和改進

---

## 🎯 第 3 部分：優化前置條件清單

### 必須完成的前置任務

在選擇方案前，以下任務 **必須** 完成：

```
優先級 🔴 高 - 本週完成:
├─ [ ] 檢查追蹤端點是否真的被調用
│       執行者: 開發人員
│       時間: 1 小時
│       方法: 瀏覽器 Network 標籤檢查
│
├─ [ ] 創建 feedback 表
│       執行者: 開發人員
│       時間: 0.5 小時
│       方法: 執行 CREATE TABLE SQL
│
└─ [ ] 澄清 Section 追蹤重複問題
        執行者: 開發人員
        時間: 0.5 小時
        方法: 代碼審視和邏輯確認

優先級 🔴 高 - 本週完成:
└─ [ ] 向利益相關者詢問統計需求
        執行者: 產品經理
        時間: 1-2 小時
        方法: 與 CRM 經理、營銷團隊訪談
        結果: 決定使用方案 A/B/C
```

### 根據前置任務結果的決策樹

```
詢問結果
├─ 回答: 「我們不需要這些統計功能」
│   └─ 決策: 執行【方案 A: 刪除】
│       └─ 時間: 5-6 小時 (下週完成)
│       └─ 收益: 刪除 ~1,300 行代碼，簡化代碼庫
│
├─ 回答: 「我們可能以後需要，但現在優先級不高」
│   └─ 決策: 執行【方案 B: 保留並標記】
│       └─ 時間: 5-6 小時 (下週完成)
│       └─ 收益: 保留功能，添加文檔，便於未來啟用
│
└─ 回答: 「我們確實需要這些報告，請建立儀表板」
    └─ 決策: 執行【方案 C: 實現儀表板】
        └─ 時間: 25-30 小時 (跨越 1-2 週)
        └─ 收益: 統計數據真正被利用，推動決策
        └─ 前提: 必須得到明確的需求和優先級確認
```

---

## 📅 第 4 部分：詳細時間表

### 根據不同方案的完整時間表

#### 方案 A: 刪除（5.5 小時）

```
週一 (前置任務)
├─ 9:00 - 10:00: 檢查追蹤端點是否被調用 [1h]
├─ 10:00 - 10:30: 創建 feedback 表 [0.5h]
├─ 10:30 - 11:00: 澄清 Section 追蹤問題 [0.5h]
└─ 11:00 - 12:00: 詢問利益相關者 [1h]

週二 (執行刪除)
├─ 9:00 - 9:30: 列出完整刪除清單 [0.5h]
├─ 9:30 - 11:30: 刪除統計端點 [2h]
├─ 11:30 - 12:30: 刪除重複的追蹤端點 [1h]
└─ 14:00 - 14:30: 清理資料庫表 [0.5h]

週二下午 + 週三 (測試和驗證)
├─ 14:30 - 16:30: 測試和驗證 [2h]
└─ 16:30 - 17:00: 文檔更新 [0.5h]

總耗時: 8.5 小時 (1.5 天)
完成日期: 週三下午
```

#### 方案 B: 保留並標記（5.5 小時）

```
週一 (前置任務) - 同方案 A
├─ 9:00 - 11:00: 前置任務 [2.5h]
└─ 11:00 - 12:00: 詢問利益相關者 [1h]

週二 (代碼標記和文檔)
├─ 9:00 - 11:00: 代碼標記 (添加 DEPRECATED 註釋) [2h]
├─ 11:00 - 12:30: 文檔化 (創建 DEPRECATED_ENDPOINTS.md) [1.5h]
├─ 14:00 - 15:00: 代碼組織 (重新結構化) [1h]
└─ 15:00 - 16:00: 測試驗證 [1h]

總耗時: 8.5 小時 (1.5 天)
完成日期: 週二下午
```

#### 方案 C: 實現儀表板（25-30 小時）

```
第 1 週:
├─ 週一 (前置 + 需求) - 5.5 小時
│  ├─ 9:00 - 12:00: 前置任務 [3h]
│  ├─ 14:00 - 17:00: 需求訪談 (CRM 經理、營銷團隊) [3h]
│  └─ 需求定義 [2h] (週末或週二分散)
│
├─ 週二 (設計) - 4 小時
│  ├─ 9:00 - 12:00: 儀表板設計 UI/UX [3h]
│  └─ 14:00 - 15:00: 設計審視和調整 [1h]
│
├─ 週三 (後端優化) - 6 小時
│  ├─ 9:00 - 12:00: 優化現有統計端點 [3h]
│  ├─ 13:00 - 17:00: 實現缺失的統計端點 [4h]
│  └─ 下班前: 測試端點
│
└─ 週四 - 週五 (前端實現) - 8 小時
   ├─ 前端開發 [8h]
   ├─ HTML + CSS + JavaScript
   └─ 日期篩選、圖表渲染、導出功能

第 2 週:
├─ 週一 - 週二 (CRM 整合) - 4 小時
│  └─ 整合到 tagger.html
│
├─ 週三 (測試和優化) - 4 小時
│  ├─ 功能測試
│  ├─ 性能優化
│  └─ 跨瀏覽器測試
│
└─ 週四 - 週五 (UAT 和迭代) - 2 小時
   ├─ 邀請實際用戶測試
   ├─ 收集反饋
   └─ 快速修復

總耗時: 25-30 小時 (3-4 天)
完成日期: 第 2 週週五
```

---

## ✅ 第 5 部分：決策清單

### 必須在開始優化前確認

- [ ] **優先級決策**: 我們是否應該立即進行優化，還是等待其他功能?
- [ ] **方案選擇**: 基於利益相關者反饋，選擇方案 A/B/C
- [ ] **資源分配**: 誰負責執行? (開發人員、產品經理)
- [ ] **測試計劃**: 如何驗證優化不會破壞現有功能?
- [ ] **文檔更新**: 優化後如何更新 API 文檔和部署指南?
- [ ] **備份計劃**: 如何應對優化過程中的意外問題?

---

## 📋 第 6 部分：風險評估

### 各方案的風險和缺減措施

#### 方案 A (刪除) 的風險

| 風險 | 可能性 | 影響 | 缺減措施 |
|------|--------|------|---------|
| 刪除的功能將來被需要 | 低 | 高 | 完整備份，記錄已刪除內容 |
| 刪除時引入 bug | 低 | 中 | 逐個刪除，每次測試 |
| 現有功能受影響 | 低 | 高 | 充分的單元測試和集成測試 |
| 資料庫表刪除導致數據丟失 | 低 | 高 | 先備份資料庫，再刪除表 |

#### 方案 B (保留) 的風險

| 風險 | 可能性 | 影響 | 缺減措施 |
|------|--------|------|---------|
| 代碼庫仍然臃腫 | 高 | 低 | 定期審視，決定是否遷移到方案 A 或 C |
| 文檔過期，開發者混淆 | 中 | 中 | 定期審視文檔，添加"最後更新"日期 |
| 未使用的端點成為技術債 | 中 | 中 | 明確標記並在開發會議中討論 |

#### 方案 C (儀表板) 的風險

| 風險 | 可能性 | 影響 | 缺減措施 |
|------|--------|------|---------|
| 需求定義不清 | 中 | 高 | 進行充分的用戶訪談，創建需求文檔 |
| 開發時間超期 | 中 | 中 | 分階段交付，優先實現核心指標 |
| 性能問題 (查詢慢) | 中 | 中 | 添加資料庫索引，實現快取 |
| 用戶不會使用儀表板 | 低 | 高 | 進行充分的 UAT，收集反饋並迭代 |
| 儀表板維護成本高 | 中 | 中 | 定期審視指標需求，移除不再使用的指標 |

---

## 🎬 第 7 部分：最終建議

### 根據當前信息的優化路徑建議

**短期 (這週)**:
1. ✅ 完成所有前置任務 (調查、詢問)
2. ✅ 根據結果決定方案
3. ✅ 準備詳細計劃

**中期 (下週)**:
- 如選擇方案 A 或 B: 執行優化 (1-2 天)
- 如選擇方案 C: 開始需求定義和設計

**長期 (後續)**:
- 如選擇方案 A: 監控用戶反饋，確保沒有漏掉重要功能
- 如選擇方案 B: 定期審視，決定何時遷移到 A 或 C
- 如選擇方案 C: 持續改進儀表板，根據用戶反饋添加新指標

### 我的建議優先順序

基於 Linus 原則（簡潔優於完美），建議順序為:

1. **首選: 方案 A (刪除)** - 如果利益相關者確實不需要統計
   - 理由: 最簡潔，技術債最少
   - 時間: 1.5 天
   - 收益: 代碼簡潔，維護簡單

2. **次選: 方案 B (保留並標記)** - 如果不確定未來是否需要
   - 理由: 風險最低，保留選項
   - 時間: 1.5 天
   - 收益: 清晰的文檔，便於未來決策

3. **最後: 方案 C (儀表板)** - 如果確實有明確的用戶需求
   - 理由: 最複雜，但價值最高
   - 時間: 1-2 週
   - 收益: 數據真正被利用，推動決策

---

**計劃完成日期**: 2025-11-14
**計劃審查日期**: 完成前置任務後 (預計 2025-11-21)
**最終決策日期**: 應在本週五 (2025-11-14) 前進行

