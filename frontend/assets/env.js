// 可在部署前覆寫這些設定，例如：
// window.ENV_API_BASE = 'https://your-backend-domain/api/v1';
// window.ENV_API_ROOT = 'https://your-backend-domain';

window.ENV_API_BASE = window.ENV_API_BASE || '';
window.ENV_API_ROOT = window.ENV_API_ROOT || '';
